import fs from 'node:fs';import path from 'node:path';const roots=['src','package.json','next.config.ts','.github','Dockerfile'];const forbidden=[/from ['\"]@supabase\//i,/supabase-js/i,/wrangler/i,/cloudflare\/workers/i,/pages\.dev/i];function files(p){if(!fs.existsSync(p))return[];const s=fs.statSync(p);if(s.isFile())return[p];return fs.readdirSync(p).flatMap(x=>files(path.join(p,x)))}let bad=[];for(const f of roots.flatMap(files)){const text=fs.readFileSync(f,'utf8');if(forbidden.some(r=>r.test(text)))bad.push(f)}if(bad.length){console.error('Prohibited active dependency/config:',bad.join(', '));process.exit(1)}console.log('prohibited-provider scan: PASS');
