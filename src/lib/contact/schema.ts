import {z} from 'zod';export const contactSchema=z.object({name:z.string().trim().min(2).max(120),company:z.string().trim().min(2).max(160),email:z.string().trim().email().max(254),phone:z.string().trim().max(40).optional().or(z.literal('')),interest:z.string().trim().min(2).max(80),message:z.string().trim().min(10).max(4000),consent:z.literal(true)});export type ContactInput=z.infer<typeof contactSchema>;
