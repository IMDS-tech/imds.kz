import {describe,expect,it} from 'vitest';import {contactSchema} from './schema';const valid={name:'Test User',company:'IMDS Test',email:'test@example.com',interest:'beles',message:'Synthetic test message',consent:true as const};describe('contact schema',()=>{it('accepts a valid lead',()=>expect(contactSchema.safeParse(valid).success).toBe(true));it('rejects invalid email',()=>expect(contactSchema.safeParse({...valid,email:'bad'}).success).toBe(false));it('requires consent',()=>expect(contactSchema.safeParse({...valid,consent:false}).success).toBe(false))});
