import {Pool} from 'pg';import type {ContactInput} from './schema';let pool:Pool|undefined;export function getWebsitePool(){const url=process.env.DATABASE_URL;if(!url)throw new Error('DATABASE_URL is not configured');return pool??=new Pool({connectionString:url,max:5})}export async function insertLead(input:ContactInput){await getWebsitePool().query('INSERT INTO website_leads (name, company, email, phone, interest, message, consent_at) VALUES ($1,$2,lower($3),nullif($4,\'\'),$5,$6,now())',[input.name,input.company,input.email,input.phone??'',input.interest,input.message])}
