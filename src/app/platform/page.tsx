import { ContentPage } from '@/components/content/ContentPage';export const metadata={title:'Платформа'};export default function Page(){return <ContentPage eyebrow="IMDS Platform" title="Одна технологическая основа." lead="Общий identity, доступы, контракты и runtime-модель связывают продукты IMDS."><h2>Единая идентификация</h2><p>SSO и платформенный контекст позволяют пользователю переходить между доступными продуктами без разрыва рабочего процесса.</p><h2>Контракты вместо общей базы</h2><p>Продукты владеют своими bounded contexts и взаимодействуют через API и события, а не через прямой доступ к чужим таблицам.</p><h2>Self-hosted runtime</h2><p>Основная production-инфраструктура работает на серверах IMDS; GitHub используется для репозиториев, Actions и GHCR.</p></ContentPage>}
