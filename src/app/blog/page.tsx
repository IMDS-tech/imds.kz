import Link from 'next/link';import {getAllPosts} from '@/lib/blog';export const metadata={title:'Блог',description:'Продуктовые и инженерные материалы IMDS TECH'};export default function Page(){const posts=getAllPosts();return <section className="section"><div className="container"><div className="page-hero"><div className="eyebrow">Insights</div><h1>Блог IMDS</h1><p className="lead">Продукты, архитектура и подходы к построению единой бизнес-платформы.</p></div><div className="card-grid">{posts.map(p=><Link className="info-card" key={p.slug} href={`/blog/${p.slug}`}><div className="eyebrow">{p.category} · {p.publishedAt}</div><h3>{p.title}</h3><p className="muted">{p.excerpt}</p></Link>)}</div></div></section>}
