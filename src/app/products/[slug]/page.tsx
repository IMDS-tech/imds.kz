import type { Metadata } from 'next';import { notFound } from 'next/navigation';import Link from 'next/link';import { products,productsBySlug } from '@/content/products';import type { ProductSlug } from '@/lib/site-config';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=productsBySlug[slug as ProductSlug];if(!p)return {};return {title:p.name,description:p.summary,alternates:{canonical:`/products/${p.slug}`},openGraph:{title:p.name,description:p.summary,url:`/products/${p.slug}`}}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=productsBySlug[slug as ProductSlug];if(!p)notFound();return <><section className="page-hero"><div className="container"><div className="eyebrow">{p.eyebrow}</div><h1>{p.headline}</h1><p className="lead">{p.summary}</p><div className="actions"><Link className="button" href="/contact">Запросить демо</Link></div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Проблема</h2><p className="lead">{p.problem}</p></div><div><h2>Решение</h2><p className="lead">{p.solution}</p></div></div><h2>Возможности</h2><div className="card-grid">{p.capabilities.map(x=><div className="info-card" key={x}><h3>{x}</h3><p className="muted">Встроено в рабочий контур {p.name}.</p></div>)}</div></div></section><section className="section dark"><div className="container"><h2>Как работает</h2><div className="card-grid">{p.workflow.map((x,i)=><div className="info-card" key={x}><div className="eyebrow">0{i+1}</div><h3>{x}</h3></div>)}</div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Интеграция с IMDS Platform</h2></div><div>{p.platformIntegration.map(x=><p key={x}>• {x}</p>)}</div></div><h2>FAQ</h2>{p.faq.map(x=><div className="faq" key={x.q}><h3>{x.q}</h3><p className="muted">{x.a}</p></div>)}</div></section></>}
