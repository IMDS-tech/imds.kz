'use client';import {useState} from 'react';export function ContactForm(){const [status,setStatus]=useState<'idle'|'sending'|'success'|'error'>('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;setStatus('sending');const f=new FormData(form);const body={name:f.get('name'),company:f.get('company'),email:f.get('email'),phone:f.get('phone'),interest:f.get('interest'),message:f.get('message'),consent:f.get('consent')==='on'};try{const res=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});if(res.ok){setStatus('success');form.reset()}else setStatus('error')}catch{setStatus('error')}}return <form className="contact-form" onSubmit={submit}><div className="field"><label htmlFor="name">Имя</label><input id="name" name="name" required minLength={2}/></div><div className="field"><label htmlFor="company">Компания</label><input id="company" name="company" required/></div><div className="field"><label htmlFor="email">Рабочий email</label><input id="email" name="email" type="email" required/></div><div className="field"><label htmlFor="phone">Телефон</label><input id="phone" name="phone" inputMode="tel"/></div><div className="field"><label htmlFor="interest">Интерес</label><select id="interest" name="interest" required><option value="platform">IMDS Platform</option><option value="beles">BELES</option><option value="mis">MIS</option><option value="resto">Resto</option><option value="other">Другое</option></select></div><div className="field"><label htmlFor="message">Задача</label><textarea id="message" name="message" required minLength={10}/></div><label><input type="checkbox" name="consent" required/> Я согласен на обработку данных для ответа на обращение.</label><button className="button" disabled={status==='sending'}>{status==='sending'?'Отправляем…':'Отправить'}</button>{status==='success'&&<p className="status" role="status">Сообщение принято.</p>}{status==='error'&&<p className="status" role="alert">Не удалось отправить. Попробуйте ещё раз.</p>}</form>}
